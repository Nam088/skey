# Shared contracts

Thư mục bổ sung này chứa schema/protocol/vector dùng chung mà không sửa cấu trúc `src/`, `port/` hoặc `macos/skey-app/`.

```text
shared-contracts/
├── config.schema.json
├── ipc.schema.json
├── localization.keys.json
├── theme.tokens.json
└── typing-vectors/
```


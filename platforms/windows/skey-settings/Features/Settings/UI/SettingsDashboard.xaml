<Page
    x:Class="SKey.Settings.SettingsDashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="32,24,32,24" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="4">
                <TextBlock
                    Text="Settings"
                    Style="{StaticResource HeaderTextBlockStyle}"
                    FontSize="32"
                    FontWeight="SemiBold" />
                <TextBlock
                    Text="Configure typing, appearance, shortcuts and tools"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap" />
            </StackPanel>

            <AutoSuggestBox
                x:Name="SettingsSearchBox"
                Grid.Column="1"
                PlaceholderText="Search settings"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                AutomationProperties.Name="Search settings" />
        </Grid>

        <!-- Current input status -->
        <Border
            Grid.Row="1"
            Padding="20,16"
            CornerRadius="10"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border
                    Width="44"
                    Height="44"
                    CornerRadius="22"
                    Background="{StaticResource SKeyAccentBrush}"
                    VerticalAlignment="Center">
                    <TextBlock
                        Text="A"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="White" />
                </Border>

                <StackPanel Grid.Column="1" Spacing="3" VerticalAlignment="Center">
                    <TextBlock Text="Vietnamese input" FontSize="16" FontWeight="SemiBold" />
                    <TextBlock
                        Text="Telex  •  Unicode  •  Ready"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>

                <ToggleSwitch
                    x:Name="VietnameseToggle"
                    Grid.Column="2"
                    Header="Enabled"
                    IsOn="True"
                    OnContent="On"
                    OffContent="Off"
                    AutomationProperties.Name="Enable Vietnamese input" />
            </Grid>
        </Border>

        <!-- Page content host -->
        <Grid Grid.Row="2" MinHeight="420">
            <Frame
                x:Name="SettingsContentFrame"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                NavigationCacheMode="Enabled" />

            <StackPanel
                x:Name="EmptySearchState"
                Visibility="Collapsed"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="8">
                <SymbolIcon Symbol="Find" FontSize="32" HorizontalAlignment="Center" />
                <TextBlock Text="No settings found" FontSize="18" HorizontalAlignment="Center" />
                <TextBlock
                    Text="Try a different search term"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Footer -->
        <Grid Grid.Row="3" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Text="Changes are saved automatically"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                VerticalAlignment="Center" />
            <Button
                Grid.Column="1"
                Content="Reset to defaults"
                AutomationProperties.Name="Reset settings to defaults" />
            <Button
                Grid.Column="2"
                Content="Open diagnostics"
                AutomationProperties.Name="Open diagnostics" />
        </Grid>
    </Grid>
</Page>

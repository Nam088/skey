# Windows feature parity

The Windows Settings frontend mirrors every feature group in
`macos/skey-app/Sources/Features`: Cleaner, Clipboard, Keyboard, Settings and
Translator. Native controls may differ, but models, commands, states and
localization keys must remain behavior-compatible.
